pipeline {
 agent any
          stages {
   //   git "https://github.com/vijayendhar/learn-git.git"
     // def x = "learngit"
     // dir(x)
   //   { 
          stage ("Four")
			 {
			 steps {
			     if($Buildchoice == "Validate")
							{
                                bat 'mvn validate'
                        	}
						else
							{
								bat 'mvn compile'
							}
                    }							
			     
			 
           }
      }
	
}